function  isLanscape(width,height) {

	// return width>height ? true : false; 
	//No need to do this while comparing and returning boolean values.

	return width>height;
}

console.log(isLanscape(600, 300));